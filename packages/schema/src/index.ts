/**
 * @wolly/schema — canonical Firestore document schema for the Wolly platform.
 *
 * This package is the single source of truth for the shapes of documents stored
 * in Firestore. The web apps (creator-hub, backoffice) import these types
 * directly; the Flutter reader mirrors them in Dart (see SCHEMA.md).
 *
 * Populated in Phase 2 of the monorepo migration.
 */

export {};
