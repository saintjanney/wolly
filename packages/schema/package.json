{
  "name": "@wolly/schema",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical Firestore document schema and shared TypeScript types for the Wolly platform \u2014 single source of truth for the creator-hub and backoffice apps.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
