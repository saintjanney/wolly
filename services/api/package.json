{
  "name": "@wolly/api",
  "version": "1.0.0",
  "private": true,
  "description": "Wolly server tier: Cloud Functions for publishing, payments, email and scheduled work.",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "npm run build && node --test test/render.test.js test/contract.test.js test/webhook.test.js"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "typescript": "^5",
    "typescript-eslint": "^8.65.0"
  }
}
